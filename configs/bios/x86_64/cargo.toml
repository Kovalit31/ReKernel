[package]
name = "rekernel_image"         # or any other name
version = "0.1.0"

[build-dependencies]
bootloader = "0.11.4"

[dependencies]
# used for UEFI booting in QEMU
ovmf-prebuilt = "0.1.0-alpha.1"
